# Installatie van opencv op virtual machine

Eerst de git binnenhalen:

```
git clone git@github.com:opencv/opencv.git
```

De juiste branch pakken:

```
git checkout 4.1.1
```

Daarna nog lib installeren:

```
sudo apt install libeigen3-dev
```

